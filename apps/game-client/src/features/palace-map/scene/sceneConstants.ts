/** World units per grid cell (X/Z spacing between tiles). */
export const TILE_SIZE = 1;

/** World Y units per cosmetic terrain height level. */
export const HEIGHT_UNIT = 0.28;

/** Thickness of a terrain tile block. */
export const TILE_THICKNESS = 0.15;

# Changelog

## [0.8.3] - 2026-05-16

### Fixed
- Unity — Période d'invincibilité au spawn pour éviter le combat immédiat après fuite
- Unity — Suppression des transitions Animator sans conditions
- Unity — URL automatique dev/prod via `#if UNITY_EDITOR`
- Unity — Position de spawn initiale correcte pour les nouveaux joueurs
- Docker — Variable `BACKEND_URL` configurée pour le proxy nginx frontend
- Seeder — Coordonnées de spawn corrigées (`Dev Island`, X:0.05, Y:0.57)

## [0.8.2.2] - 2026-05-15

### Fixed
- `GameSavesController` — Suppression du champ `AdminRole` local redondant
- `DatabaseSeeder` — Remplacement des literals par les constantes du Domain
- `PlayerSkillRepository` — Suppression de l'instruction vide ligne 20
- Tests — `null!` pour les paramètres non-nullable dans `GuardTests` et `Argon2PasswordHasherTests`

## [0.8.2.1] - 2026-05-14

### Fixed
- `Program.cs` — `app.Run()` remplacé par `await app.RunAsync()`
- Controllers — Constante `AdminRole` centralisée dans `Constants.cs`
- `RGPDController` — Renommé en `RgpdController` (Pascal case)
- `Guard.cs` — Surcharges `ThrowIfNullOrEmpty` regroupées
- `Result.cs` — Surcharges `Success` et `Failure` regroupées
- `AppDbContext` — Literal `jsonb` remplacé par constante
- Railway — `ASPNETCORE_ENVIRONMENT` corrigé en `Production`

## [0.8.2] - 2026-05-13

### Fixed
- Backend — Suppression des `Console.WriteLine` du seeder
- Backend — `JsonSerializerOptions` en `static readonly`
- Backend — `GC.SuppressFinalize` ajouté dans tous les `Dispose()` de tests
- Backend — Méthodes helper de tests rendues `static`
- CI — Exclusion des migrations de l'analyse SonarCloud
- CI — Filtres `paths` ajoutés sur backend-ci et frontend-ci

## [0.8.1] - 2026-05-12

### Fixed
- Backend — Secrets sortis des fichiers de configuration
- Backend — `dotnet user-secrets` configuré pour le développement local
- Backend — `ValidateOnStart` sur les options JWT
- Frontend — Configuration ESLint ajoutée
- Docker — Variable `BACKEND_URL` pour nginx
- README — Mise à jour complète

## [0.8.0] - 2026-05-10